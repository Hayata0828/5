<h1><%= @product.title %>の編集</h1>

<%= form_for @product, url: "/product/#{@product.id}/update" do |f| %>
    <%= f.label :"Title" %><br>
    <%= f.text_area :title %><br>
    <%= f.label :"Price" %><br>
    <%= f.text_area :price %><br>
    <%= f.label :"Description" %><br>
    <%= f.text_area :description %><br>
    <%= f.hidden_field :manager_id, value: current_manager.id %>

    <%= f.fields_for (:product_images) do |image| %>
      <%= image.attachment_field :image %>
    <% end %>
    <%= f.fields_for (:product_images) do |image| %>
      <%= image.attachment_field :image %>
    <% end %>
    <%= f.fields_for (:product_images) do |image| %>
      <%= image.attachment_field :image %>
    <% end %>
    
    <%= f.submit(["Up date"]) %>
<% end %>